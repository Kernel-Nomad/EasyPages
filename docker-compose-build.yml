# EasyPages — build from repo: docker compose -f docker-compose-build.yml up -d --build
# Same as docker-compose.yml; see README.md.
name: easypages
services:
  easypages:
    container_name: easypages
    build:
      context: .
      dockerfile: Dockerfile
    image: easypages:local
    restart: unless-stopped
    ports:
      - "8002:8002"
    environment:
      EASYPAGES_DATA_DIR: /data
      TRUST_PROXY: "false"
    env_file:
      - .env
    volumes:
      - ./easypages-data:/data
    # The Dockerfile carries its own HEALTHCHECK; this repeats it so anyone holding an
    # older docker-compose.yml can see the right path. /api/health is public and does not
    # touch the session, unlike /api/auth/status.
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:8002/api/health').then(r=>process.exit(r.ok?0:1)).catch(()=>process.exit(1))",
        ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
