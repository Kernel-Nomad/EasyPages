# 1) Required
CF_API_TOKEN=xxxxx

# 2) Optional
# CF_ACCOUNT_ID=xxxxx
# EASYPAGES_DATA_DIR=/data   # Session secret and credentials. Docker sets this already.
# PORT=8002
# SESSION_COOKIE_SECURE=false
# TRUST_PROXY=false          # Set when Node is exposed without a trusted reverse proxy
